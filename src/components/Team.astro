---
// src/components/Team.astro
import '../styles/team.css';

const team = [
  {
    name: "Dra. Ana López",
    role: "Psicóloga Clínica",
    bio: "Especialista en terapia cognitivo-conductual con 10 años de experiencia.",
    photo: "/images/team1.jpg",
    social: [
      { platform: 'linkedin', url: '#' },
      { platform: 'email', url: 'mailto:ana@ejemplo.com' }
    ]
  },
  {
    name: "Lic. Carlos Méndez",
    role: "Terapeuta Familiar",
    bio: "Experto en dinámicas familiares y resolución de conflictos.",
    photo: "/images/team2.jpg",
    social: [
      { platform: 'linkedin', url: '#' },
      { platform: 'website', url: '#' }
    ]
  },
  {
    name: "Mtra. Sofía Ramírez",
    role: "Especialista en Trauma",
    bio: "Certificada en EMDR y terapias de tercera generación.",
    photo: "/images/team3.jpg",
    social: [
      { platform: 'researchgate', url: '#' },
      { platform: 'email', url: 'mailto:sofia@ejemplo.com' }
    ]
  }
];
---

<section class="team" id="equipo" aria-labelledby="team-heading">
  <div class="container">
    <h2 id="team-heading" class="section-title" data-animate="fade-up">Nuestro Equipo</h2>
    
    <div class="team-grid">
      {team.map((member, index) => (
        <article class="team-card" data-animate="fade-up" data-animate-delay={index * 100}>
          <div class="card-inner">
            <div class="card-front">
              <img 
                src={member.photo} 
                alt={`Retrato de ${member.name}`} 
                class="member-photo"
                loading="lazy"
              />
              <div class="member-info">
                <h3 class="member-name">{member.name}</h3>
                <p class="member-role">{member.role}</p>
              </div>
            </div>
            
            <div class="card-back">
              <p class="member-bio">{member.bio}</p>
              <div class="social-links">
                {member.social.map(link => (
                  <a 
                    href={link.url} 
                    class={`social-link ${link.platform}`}
                    target="_blank"
                    rel="noopener noreferrer"
                  >
                    {link.platform === 'email' ? '✉️' : ''}
                    {link.platform === 'linkedin' ? '👔' : ''}
                    {link.platform === 'website' ? '🌐' : ''}
                    {link.platform === 'researchgate' ? '📚' : ''}
                  </a>
                ))}
              </div>
            </div>
          </div>
        </article>
      ))}
    </div>
  </div>
</section>